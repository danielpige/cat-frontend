import { TranslateBreedKeyPipePipe } from './translate-breed-key-pipe.pipe';

describe('TranslateBreedKeyPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateBreedKeyPipePipe();
    expect(pipe).toBeTruthy();
  });
});
